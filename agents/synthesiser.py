"""
agents/synthesiser.py  — Caption Synthesiser Agent (Day 8-9)
agents/judge.py        — Judge Agent (Day 10-11)

Both are stubs. Full implementation in Week 2.
"""

from loguru import logger


class CaptionSynthesiser:
    """Caption Synthesiser — Day 8-9 implementation placeholder."""

    def __init__(self) -> None:
        logger.info("CaptionSynthesiser initialised (stub — full impl Day 8-9)")

    def run(self, narrator_output, rag_output) -> str:
        raise NotImplementedError(
            "CaptionSynthesiser.run() — implementation scheduled for Day 8-9."
        )
